# Corpora

Repo-owned data roots live here. Large audio, archives, extracted corpora, and
generated eval artifacts are ignored by git. The manifests and sync scripts are
tracked so the corpus layout is reproducible from a fresh checkout.

For recorder evals, use `data/corpora/recorder/`.
